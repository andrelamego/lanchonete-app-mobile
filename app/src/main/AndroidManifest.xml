<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LanchoneteMobile">
        <activity
            android:name=".adapters.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".adapters.ui.GerenciarProdutoActivity" />
        <activity android:name=".adapters.ui.FuncionarioActivity" />
        <activity android:name=".adapters.ui.GerenciarClienteActivity" />
        <activity android:name=".adapters.ui.PedidoActivity" />
        <activity android:name=".adapters.ui.GerenciarFornecedorActivity" />
        <activity android:name=".adapters.ui.GerenciarCargoActivity" />
        <activity android:name=".adapters.ui.GerenciarCategoriaActivity" />
        <activity android:name=".adapters.ui.GerenciarPedidoActivity" />
        <activity android:name=".adapters.ui.GerenciarFuncionarioActivity" />
        <activity android:name=".adapters.ui.FormPedidoActivity" />
    </application>

</manifest>